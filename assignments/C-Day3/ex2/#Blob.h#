#ifndef _BLOB
#define _BLOB

#include "ColoredShape.h"

class Blob public ColoredShape {
 public:
  Circle(double r, string color);
  ~Circle();
  double getArea(void);

 protected:

 private:
  double r;
  double getPI(void);
};

#endif // _CIRCLE

  
